services:
  ros_ws:
    build:
      context: .
      dockerfile: Dockerfile
      args:
        - "UID=$DOCKER_UID"
        - "GID=$DOCKER_GID"
    privileged: true
    restart: no
    command: tail -f /dev/null
    volumes:
      - .:/home/rosdev/ros2_ws/
