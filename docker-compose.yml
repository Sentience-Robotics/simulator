services:
  ros_ws:
    build:
      context: .
      dockerfile: Dockerfile
      args:
        - "UID=$DOCKER_UID"
        - "GID=$DOCKER_GID"
    restart: no
    command: tail -f /dev/null
    volumes:
      - .:/home/rosdev/ros2_ws/src/inmoov_ros_sim
