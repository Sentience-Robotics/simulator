services:
  ros_ws:
    image: ghcr.io/sentience-robotics/ros2_base:humble
    environment:
      - PUID=$DOCKER_UID
      - PGID=$DOCKER_GID
    ports:
      - "3000:3000"
    restart: unless-stopped
    volumes:
      - .:/config/ros2_ws/
